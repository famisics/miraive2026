// Turso (default)
export { db, userRepository, insertUserSchema, selectUserSchema } from '@packages/db-turso'
export type { User, NewUser } from '@packages/db-turso'

// Supabase
// export { db, userRepository, insertUserSchema, selectUserSchema } from '@packages/db-supabase'
// export type { User, NewUser } from '@packages/db-supabase'
